{% extends 'layout.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-lg-3">{{ render(controller('AppBundle:Profile:show')) }}</div>
            <div class="col-lg-9">
                {% block content %}{% endblock %}
            </div>
        </div>
    </div>
{% endblock %}